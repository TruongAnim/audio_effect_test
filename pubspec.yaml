name: audio_effect_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  # audioplayers: ^5.0.0
  audio_session: ^0.1.16
  flutter:
    sdk: flutter
  flutter_lyric: ^2.0.4+6
  flutter_sound: ^9.2.13
  get: ^4.6.5
  just_audio: ^0.9.34
  path_provider: ^2.0.15
  record: ^4.4.4
  video_player: ^2.7.0
  firebase_core: ^2.9.0
  cloud_firestore: ^4.5.1
  firebase_storage: ^11.1.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/videos/
    - assets/images/
    - assets/bg.jpeg
